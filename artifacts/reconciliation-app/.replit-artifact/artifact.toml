kind = "web"
previewPath = "/"
title = "Agricultural Reconciliation"
version = "1.0.0"
id = "artifacts/reconciliation-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25745

[services.development]
run = "pnpm --filter @workspace/reconciliation-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/reconciliation-app", "run", "build" ]
publicDir = "artifacts/reconciliation-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25745"
BASE_PATH = "/"
